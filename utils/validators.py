"""
utils/validators.py — Input validation helpers.

Validates:
- Crop names (uniqueness, non-empty)
- Distribution percentages (sum to 100% per category)
- Garden configuration (positive bed counts, valid dimensions)
- Cycle identifiers (correct format per cycles_per_year setting)
- Override data (valid crop/category combinations)
"""

# Implementation will be added in a future session.
